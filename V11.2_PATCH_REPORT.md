# V11.2 패치 보고서

반영 내용
- `thisMonth` 호출 누락 수정: `thisMonth` -> `thisMonth()`
- T8 서식 탭 진입 시 `mgr-ym`, `pay-ym`, `exec-ym` 기본값 설정 보강
- T8 서식 탭 진입 시 `pay-stf-sel` 첫 유효 옵션 자동 선택 복구
- `window.loadVerify`를 지원단 개인별 월단위 검증 버전으로 유지
- `window.printMgrBook`, `window.printTT` 최종 핸들러를 새창 인쇄 방식으로 고정
- Service Worker 캐시 이름을 `jc-edu-clinic-v11.2-cache`로 갱신
- `title`, 헤더, 로그인 보조 버전 표기를 V11.2로 통일
- `saveCfg` 최종 보정으로 `confirmer` 기본값을 `담당 장학사`로 재확정

검사 결과
- JS 문법 검사 통과 (`node --check`)
- 세트 파일 ZIP 생성 완료
- 싱글파일 생성 완료
