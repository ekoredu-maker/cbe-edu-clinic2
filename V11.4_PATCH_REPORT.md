# V11.4 Patch Report

반영 사항
- T5 탭을 `실적 입력 / 월간 검증 / 월별 정산` 구조로 변경
- 지원단별 월 실적 조회, 추가, 수정, 삭제 기능 추가
- T5 진입 시 지원단 드롭다운/월 기본값 자동 초기화
- `buildSettleData()`에서 수업협력(`kind===class`) 학교/학급 표시 보정
- `exportExecReportXlsx()`를 K-에듀파인 업로드용 열 구조로 수정
- Service Worker 캐시명을 `jc-edu-clinic-v11.4-cache`로 변경
- 버전 표기를 V11.4로 통일

비고
- 기존 `오늘의 실시기록` 로직은 파일 내에 남아 있으나, UI와 탭 진입 흐름은 `실적 입력` 기준으로 전환되었습니다.
- 실적 수정/삭제는 미검증(`conducted`) 상태에서만 허용됩니다.
