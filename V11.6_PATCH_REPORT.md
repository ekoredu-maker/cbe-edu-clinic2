# V11.6 PATCH REPORT

## 반영 내용
- `06-statistics.js`
  - `pivotByGrade()`를 검증 완료(`verified/paid`) 실적 기반으로 재집계하도록 수정
  - `pivotByRegion()`를 검증 완료(`verified/paid`) 실적 기반으로 재집계하도록 수정
  - `collectActualExecutionMetrics()` 추가
  - `inferRegionFromClassInfo()` 추가
- `12-patches.js`
  - 버전 주석 갱신
  - `saveEditRec()` 존재 재확인 및 누락 시 보강 가능 구조 유지
- `sw.js`
  - 캐시명 `jc-edu-clinic-v11.6-cache`로 갱신
- `index.html`
  - 표시 버전 `V11.6 GitHub Edition`으로 갱신

## 의도
- 통계 탭이 학생 등록 데이터만 세는 문제를 줄이고, 실제 검증 완료된 실적과 더 가깝게 맞춤
- 수업협력(class) 매칭의 학교/학급 정보가 지역·학년 통계에 반영되도록 보정

## 참고
- `class_stu`는 데이터 모델상 반 학생 수가 직접 저장되지 않아, 같은 학교/학교급/학년/반으로 연결 가능한 학생 레코드 기준으로 집계합니다.
