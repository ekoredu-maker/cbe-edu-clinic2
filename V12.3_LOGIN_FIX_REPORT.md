# V12.3 login fix
- Corrected the delivered standalone HTML so it is truly self-contained.
- Explicitly exposed `window.doLogin` / `window.doLogout` for inline button handlers.
- Added a JavaScript SHA-256 fallback when `crypto.subtle` is unavailable.
- Updated set build to V12.3 and bumped Service Worker cache to `jc-edu-clinic-v12.3-cache`.
