# V12.4 term-based application update

- 학생 신청을 연간 1건이 아니라 `신청년도 + 신청회차(1학기/여름방학/2학기/겨울방학)` 기준으로 저장하도록 변경했습니다.
- 기존 학생 데이터는 마이그레이션 시 `현재 학년도 + 1학기` 기본값으로 보정됩니다.
- 학생 신청 양식/업로드/다운로드에 `신청년도`, `신청회차` 열을 추가했습니다.
- 학생 신청 목록, 매칭 카드, 수동 매칭 모달, 실적 입력에서 회차 정보가 표시됩니다.
- 통계는 기존처럼 신청 레코드를 누적 집계하므로, 동일 학생이 4개 회차를 모두 신청하면 지원현황에 4건으로 반영됩니다.
