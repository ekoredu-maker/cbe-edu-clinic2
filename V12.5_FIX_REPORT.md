# V12.5 Fix Report

- 실적/검증 탭 순서를 `월간 검증 -> 실적 입력 -> 월별 정산`으로 변경
- 실적 추가 모달에서 지원단별 활성 매칭 조회를 `String(stfId)` 비교로 보강
- 활성 매칭이 없을 때 명확한 경고 메시지 표시
- 실적 입력 조회도 지원단 ID 타입 차이(number/string)를 허용하도록 보강
- 인쇄 헬퍼를 수정하여 인쇄 창에서 `print-root`만 보이도록 강제해 빈 페이지 문제 완화
- 팝업/iframe 인쇄 대기시간을 늘려 스타일 로드 후 인쇄되도록 조정
- Service Worker 캐시명을 `jc-edu-clinic-v12.5-cache`로 갱신
